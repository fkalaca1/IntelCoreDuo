<UserControl
    x:Class="ParKing.View.UserControls.DateTimeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ParKing.View.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="50">

    <StackPanel Orientation="Horizontal">

        <StackPanel Orientation="Horizontal" Background="White">

            <Button Style="{StaticResource DateTimeControlButton}" Content="Od" BorderThickness="0" x:Name="OdButton">
                <Button.Flyout>
                    <DatePickerFlyout x:Name="OdDatePickerFlyout"  DatePicked="DatePickerFlyout_DatePicked" Placement="Full"/>
                </Button.Flyout>
            </Button>

            <Button Style="{StaticResource DateTimeControlButton}" Content="Do" BorderThickness="0" x:Name="DoButton">
                <Button.Flyout>
                    <DatePickerFlyout x:Name="DoDatePickerFlyout"  DatePicked="DatePickerFlyout_DatePicked" Placement="Full"/>
                </Button.Flyout>
            </Button>

        </StackPanel>

    </StackPanel>
</UserControl>
